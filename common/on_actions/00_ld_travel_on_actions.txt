# NOTE: These on_action names MUST match your vanilla.
# If yours use different names, replace them accordingly.

on_travel_cancelled = {
    events = { ld.2400 }
}

on_travel_arrived = {
    events = { ld.2401 }
}
