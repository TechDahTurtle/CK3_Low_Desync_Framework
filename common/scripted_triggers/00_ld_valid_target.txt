ld_valid_target = {
    exists = scope:target
    scope:target = {
        is_alive = yes
        NOT = { is_imprisoned = yes } # optional: remove if you want imprisoned targets to remain valid
    }
}
