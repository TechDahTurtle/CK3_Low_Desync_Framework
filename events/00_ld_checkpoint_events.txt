namespace = ld

character_event = {
    id = ld.3000
    hide_window = yes
    is_triggered_only = yes

    immediate = {

        if = {
            limit = { ld_is_mp_safe_mode = yes }

            every_player = {
                every_scheme = {
                    limit = {
                        scheme_target_character = { is_alive = no }
                    }
                    end_scheme = yes
                }
            }
        }
    }
}
